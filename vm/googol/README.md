# googol (Google VM)

This is used for:
- Surpass CGNAT for services not exposable in Cloudflare (like streaming)
- Bypass georestrictions (like Exa API not available in VE)

It is accessed through tailscale (tailnet) from gateway LXC.
